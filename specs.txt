snATAC-Express is a workflow which does the following:

1. Pseudobulks snATACseq and snRNAseq data. 
2. Model GEX ~ ATAC data multiple ways:
    - linear regression, random forest regression, XGBoost, and LightGBM
    - using ATAC peaks in 10% of samples, 50% of samples, and then all peaks (peaks must be within defined cis regulatory window of the gene)
    - Parameters are optimized via grid search; these are used in step 4
3. The feature ranks and R2 values are aggregated, including and excluding linear regression for ensembling
4. The features which explain ~95% of variance of gene expression are selected, then the models are rerun and their R2 values are evaluated.

-----------------------------------------------
Here are the scripts. I have them written to do the following in two phases:

scripts_phase1 (iterative refinement)
1. data_preprocessing.py:
    pseudobulk snRNAseq and snATACseq data (example in example_data dir): get info for each gene (gene expression and ATAC peaks)
2. model_builders.py:
    This script contains functions which builds models and ranks features. 
    The general workflow is to use nested k-fold cross validation for hyperparameter tuning and model evaluation. 
    Models included: multivariate regression, random forest regression, XGBoost, LightGBM
3. feature_selection.py:
    This script contains feature ranking functions to be imported to the main predict_gex.py script.
4. run_multites.py:
    This script runs many predictive models and executes the workflow.
5. summarize_cv_results.sh: This script goes through the results and summarizes the cross validation results


scripts_phase2 (model run and evaluation)
1. data_preprocessing.py:
    pseudobulk snRNAseq and snATACseq data (example in example_data dir): get info for each gene (gene expression and ATAC peaks)
2. model_builders.py:
    This script contains functions which builds models and ranks features. 
    The general workflow is to use nested k-fold cross validation for hyperparameter tuning and model evaluation. 
    Models included: multivariate regression, random forest regression, XGBoost, LightGBM
3. feature_selection.py:
    This script contains feature ranking functions to be imported to the main predict_gex.py script.
4. run_multites.py:
    This script runs many predictive models and executes the workflow.
5. summarize_cv_results.sh: This script goes through the results and summarizes the cross validation results
# for models using all peaks or 95% selected peaks


-----------------------------------------------
Between phases 1 and 2:
1.  data_preprocessing.py script is the same
2.  data_preprocessing.py script is the same
3. model_builders.py are different:

Working Directory Change:
Apply to specs.txt
)
The directory path has been updated to include "Aggreg" and a new date (02132024).
Cross-validation Implementation:
Phase 1 uses StratifiedKFold for cross-validation
Phase 2 uses regular KFold for cross-validation
This change appears in all the k-fold cross-validation functions (init_RFR_kfold_crossval, init_XGB_kfold_crossval, init_LR_kfold_crossval, init_LGBM_kfold_crossval).
Model Building Functions:
The model building functions (build_RFR_model, build_LR_model, build_XGB_model, build_LGBM_model) in Phase 2 have been simplified:
Removed the code for selecting top 95% of features
Removed the second model training with selected features
Removed the second cross-validation step
Only saves results for the full model, not the feature-selected model
File Structure:
Phase 1 has more extensive output generation, including separate directories for all peaks and top 95% peaks
Phase 2 has a simpler output structure, only saving results for the full model
These changes suggest that Phase 2 is a simplified version that focuses on the full model without the feature selection step, while Phase 1 includes both full model and feature-selected model training and evaluation.

4. run_multites.py are different::

Command Line Arguments:
Apply to specs.txt
)
Phase 2 replaces the individual matrix inputs with a single aggregated peaks directory.
Working Directory:
Apply to specs.txt
)
The directory path has been updated to include "Aggreg" and a new date.
Data Loading and Processing:
Phase 1:
Loads raw peak and gene expression matrices
Performs pseudobulk calculations
Filters peaks based on presence in 10% of samples
Phase 2:
Loads pre-processed data from previous run
Uses aggregated peak importance scores
Selects top 95% of peaks based on aggregated z-scores
Only runs models if aggregated results exist
Model Building Logic:
Phase 1:
Runs models on all peaks that pass the 10% presence filter
Has commented-out feature selection code
Includes a commented-out check_peaks function
Phase 2:
Only runs models if aggregated results exist
Uses pre-selected top 95% important peaks
No feature selection code
No check_peaks function
Output Directory Structure:
Apply to specs.txt
"
The main conceptual difference is that Phase 2 is designed to work with pre-processed and aggregated data from Phase 1, focusing on the most important peaks (top 95%) rather than running the full analysis pipeline from scratch. This suggests Phase 2 is a refinement or optimization step that builds upon the results of Phase 1.

5. summarize_cv_results.sh are different:
Yes, there are key differences between these two bash scripts. Both scripts summarize cross-validation results, but they handle the data differently:
Data Selection:
First script:
Apply to specs.txt
Run
"
Reads from line 2 of the results file
Outputs results with "All_Peaks" category
Second script:
Apply to specs.txt
Run
"
Also reads from line 2 of the results file
Outputs results with "Select_Peaks" category
Output Format:
First script:
Apply to specs.txt
Run
txt
Labels the output as "All_Peaks"
Second script:
Apply to specs.txt
Run
txt
Labels the output as "Select_Peaks"
The main difference is that the first script is designed to summarize results from models using all peaks, while the second script is designed to summarize results from models using selected peaks. However, both scripts are reading from line 2 of the results file, which suggests they might be processing different types of result files or the file structure has changed between phases.
This aligns with the differences we saw in the Python scripts, where Phase 1 worked with all peaks and Phase 2 worked with selected peaks.

-----------------------------------------------

I need help with the following:
1. Describe the input required for data_preprocessing.py and add it to the readme: 
    group_coverages.csv is from ArchR after creating pseudobulk replicates and running the peak calling step
    GEX and ATAC data stored as sparse matrices which are used to pseudobulk (use needs to create these, please describe the files and the format)
    I have example inputs in the example_data directory - so if there is anything else worth describing regarding the input for the user please add it to the readme
2. Organize the scripts. I have some which are for phase1, some which are for phase 2, and some which are the same for both (under scripts). 
    For the scripts which are different between phase 1 and phase 2, is there a way to combine them and create a config file to account for the difference for user input?
    I'd like to create either a wrapper script or a jupyter notebook tutorial that executes: phase 1 and then phase 2
    I'd also like to remove any hardcoding of paths
